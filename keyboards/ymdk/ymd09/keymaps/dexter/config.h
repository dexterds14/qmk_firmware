#pragma once

// Bottom-row OSL keys: tap = one-shot, second tap = lock, tap again =
// unlock (hold = momentary is built into OSL). Same flow the Panda20
// converged on.
#define ONESHOT_TAP_TOGGLE 2
